# citation：引用溯源与引用详情

目标：让 RAG 回答返回结构化引用，并支持前端点击查看来源详情和下载原文。

## 最小任务

- [ ] 新增 `app/services/citation_service.py`
- [ ] 定义引用候选模型
- [ ] 定义引用响应模型
- [ ] 将 RAG 检索切片转换为按排序编号的引用候选
- [ ] 在知识问答 Prompt 中要求使用 `[1]`、`[2]` 引用标记
- [ ] 实现引用去重，同一 `chunk_id` 只保留一个编号
- [ ] 实现回答引用标记校验，过滤不存在的引用编号
- [ ] 实现引用记录写入 `chat_message_citations`
- [ ] 修改问答完成事件，在 `done` 中返回 `answer` 和 `citations`
- [ ] 实现 `GET /api/messages/{message_id}/citations`
- [ ] 实现引用详情响应包含知识库、文档、章节、切片、分数
- [ ] 实现原文下载 URL 生成
- [ ] 前端渲染回答中的 `[1]`、`[2]` 为可点击引用标记
- [ ] 前端实现引用详情弹窗或侧边抽屉
- [ ] 前端实现原文下载入口
- [ ] 新增测试：同一 chunk 多次引用只生成一个编号
- [ ] 新增测试：引用编号与候选排序一致
- [ ] 新增测试：引用详情字段完整
- [ ] 新增测试：无资料回答不返回 citations
- [ ] 新增测试：引用下载接口校验用户权限

## 完成标准

- [ ] RAG 回答正文可显示引用编号
- [ ] 点击引用可看到来源详情
- [ ] 引用信息来自真实检索切片，不由模型自由编造

## 完成记录

- 完成时间：2026-06-28
- 主要改动：新增引用服务和 `/api/messages/{message_id}/citations`；RAG 完成时持久化引用，响应中返回文档、章节、切片、分数和下载 URL。
- 测试命令：`.\.venv\Scripts\python.exe -m pytest`
- 测试结果：`17 passed`
- 实现备注：前端已保留引用数据展示入口，引用编号点击详情后续可继续增强为侧边抽屉。
