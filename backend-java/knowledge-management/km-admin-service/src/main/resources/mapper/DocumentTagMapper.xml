<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.km.admin.document.mapper.DocumentTagMapper">

    <insert id="insert">
        INSERT INTO km_document_tag (doc_id, tag_name, created_at)
        VALUES (#{docId}, #{tagName}, NOW())
    </insert>

    <insert id="batchInsert">
        INSERT INTO km_document_tag (doc_id, tag_name, created_at)
        VALUES
        <foreach collection="tagNames" item="tag" separator=",">
            (#{docId}, #{tag}, NOW())
        </foreach>
    </insert>

    <select id="selectTagNamesByDocId" resultType="string">
        SELECT tag_name
        FROM km_document_tag
        WHERE doc_id = #{docId}
        ORDER BY id
    </select>

    <delete id="deleteByDocId">
        DELETE FROM km_document_tag WHERE doc_id = #{docId}
    </delete>
</mapper>
